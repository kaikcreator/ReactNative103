//custom native components import
import { requireNativeComponent } from 'react-native';

//export our custom native view as a react component
export default requireNativeComponent('UserInputView', null);
